export const getters = {

    posts(state) {
        return state.posts
    },

    posts_loading(state) {
        return state.posts_loading
    },

    pagination(state) {
        return state.pagination
    }

}